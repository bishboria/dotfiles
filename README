Installation:
  git clone git://github.com/bishboria/dotvim.git ~/.vim
  git clone git://github.com/gmarik/vundle.git ~/.vim/vundle.git

Create symlinks:
  ln -s ~/.vim/vimrc ~/.vimrc
  ln -s ~/.vim/gvimrc ~/.gvimrc
